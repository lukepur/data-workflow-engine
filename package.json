{
  "name": "data-workflow-engine",
  "version": "0.0.7",
  "description": "A data validation and state engine",
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/lukepur/data-workflow-engine.git"
  },
  "scripts": {
    "build": "node_modules/.bin/babel src --presets babel-preset-es2015 --out-dir dist",
    "test": "mocha --recursive",
    "test:debug": "mocha --recursive --debug",
    "test:watch": "mocha --recursive --watch",
    "test:watch:debug": "mocha --recursive --watch --debug-brk",
    "prepublish": "npm run build"
  },
  "keywords": [
    "data",
    "state",
    "engine"
  ],
  "author": "Luke Purton",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-preset-es2015": "^6.22.0",
    "chai": "^3.5.0",
    "js-yaml": "^3.7.0",
    "mocha": "^3.2.0"
  },
  "dependencies": {
    "lodash": "^4.17.4",
    "toposort": "^1.0.2",
    "traverse": "^0.6.6"
  }
}
